
import { z } from "zod";

const createcompanySchema = z.object({
	
});
const updatecompanySchema = z.object({
	
});

export const CompanyValidations = {
	createcompanySchema,
    updatecompanySchema,
};

