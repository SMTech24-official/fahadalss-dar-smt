
import { z } from "zod";

const createClientSchema = z.object({
	
});
const updateClientSchema = z.object({
	
});

export const ClientValidations = {
	createClientSchema,
    updateClientSchema,
};

